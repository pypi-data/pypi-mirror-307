
[build-system]
requires = ["setuptools>=61.0", "versioningit"]
build-backend = "setuptools.build_meta"


# ----------------------------------------- setuptools -------------------------------------------
[tool.setuptools]
include-package-data = true
zip-safe = false

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true


# ----------------------------------------- Project Metadata -------------------------------------
#
[project]
version = "0.0.0.dev18"
name = "LicenseMan"
requires-python = ">=3.10"
dependencies = [
    "LoggerMan == 0.0.0.dev61",
    "PyLinks == 0.0.0.dev45",
    "PkgData == 0.0.0.dev5",
    "PySerials == 0.0.0.dev35",
    "MDit == 0.0.0.dev32",
    "ExceptionMan == 0.0.0.dev32",
    "platformdirs >= 4.3, < 5",
]
