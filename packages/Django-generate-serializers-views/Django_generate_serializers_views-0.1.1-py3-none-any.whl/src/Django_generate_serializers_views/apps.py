from django.apps import AppConfig

class Django_generate_serializers_viewsConfig(AppConfig):
    name = 'Django_generate_serializers_views'

    def ready(self):
        # Code to run when the app is ready
        pass
