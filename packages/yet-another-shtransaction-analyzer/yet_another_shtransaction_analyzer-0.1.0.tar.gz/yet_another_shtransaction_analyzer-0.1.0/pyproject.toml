[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yet_another_shtransaction_analyzer"
version = "0.1.0"
authors = [
  { name="Your Name", email="your.email@example.com" }
]
dependencies = ["pandas"]

[project.scripts]
transaction_analyzer = "transaction_analyzer.analyzer:main"