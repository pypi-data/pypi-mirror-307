[build-system]
requires = ["setuptools>=64", "setuptools_scm>=8", "Flake8-pyproject"]
build-backend = "setuptools.build_meta"

[project]
name = "pymoku"
dynamic = ["version"]
requires-python = ">= 3.9"
classifiers = [
    "License :: OSI Approved :: MIT License",
]

[tool.setuptools_scm]
