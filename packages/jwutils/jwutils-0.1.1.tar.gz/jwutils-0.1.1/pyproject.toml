[project]
name = "jwutils"
version = "0.1.1"
description = "Personal package for fun"
readme = "README.md"
authors = [
    { name = "Wang, Jie", email = "ifendo@outlook.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.2.3",
    "polars>=1.12.0",
    "requests>=2.32.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
