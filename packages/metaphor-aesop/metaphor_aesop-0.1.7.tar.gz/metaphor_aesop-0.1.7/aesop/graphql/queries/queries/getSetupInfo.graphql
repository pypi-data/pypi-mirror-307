query getSetupInfo {
    setupInfo {
        oidc {
            signInRedirectUrl
        }
        saml {
            entityId
            replyACSUrl
            signOnUrl
        }
        crawlerIpAddresses
    }
}
