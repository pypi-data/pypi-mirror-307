query getNonProdSettings {
  settings {
    nonProd {
      datasetPatterns {
        platform
        account
        database
        schema
        table
        isCaseSensitive
      }
    }
  }
}
