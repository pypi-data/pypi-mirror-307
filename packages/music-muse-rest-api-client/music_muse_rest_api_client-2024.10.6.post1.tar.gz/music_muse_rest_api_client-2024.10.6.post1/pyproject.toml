[tool.poetry]
name = "music-muse-rest-api-client"
version = "2024.10.06-1"
description = "A client library for accessing MusicMuse REST API"
authors = []
readme = "README.md"
packages = [
    {include = "music_muse_rest_api_client"},
]
include = ["CHANGELOG.md", "music_muse_rest_api_client/py.typed"]


[tool.poetry.dependencies]
python = "^3.11"
httpx = ">=0.20.0,<0.28.0"
attrs = ">=21.3.0"
python-dateutil = "^2.8.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["F", "I", "UP"]
