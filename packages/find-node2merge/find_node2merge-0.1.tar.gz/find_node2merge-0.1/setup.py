from setuptools import setup, find_packages

setup(
    name='find_node2merge',
    version='0.1',
    author='zsy',  # 开发者名字
    description="Find potentially similar nodes in a knowledge graph.",
    packages=find_packages(),
)