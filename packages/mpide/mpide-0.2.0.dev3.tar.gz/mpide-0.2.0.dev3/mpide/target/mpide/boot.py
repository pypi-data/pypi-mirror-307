"""Entry point after a reset - does anything needed to establish a connection and invokes the
main application functionality"""
# pylint: disable=import-error

import main  # type: ignore[import-not-found]

main.run()
