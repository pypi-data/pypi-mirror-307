[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "is_star"
description = "Python package to check if a polygon is a star"
dependencies = [
    "numpy",
    "matplotlib",
]
version = "0.0.3"